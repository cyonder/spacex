import React, { Component } from 'react';

class Dates extends Component{

    render(){
        console.log("Dates-props: ", this.props);

        return(
            <div id="dates">Dates Page</div>
        )
    }
}

export default Dates;
